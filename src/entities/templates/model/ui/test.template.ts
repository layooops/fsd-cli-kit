export const testTemplate = `describe("test example", () => {
  describe("example", () => {
    it("should be a string", () => {
      expect(typeof "test example").toBe("string");
    });
  });
});`;
